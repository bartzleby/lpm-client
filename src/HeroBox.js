//
//
//

import React, { useState, useEffect } from 'react';

function HeroBox() {
    const [stack, setStack] = useState(300);
    const [hand, setHand] = useState(["Ts", "Js"]);

    const [action, setAction] = useState([]);


    return <div></div>

}

export default HeroBox;